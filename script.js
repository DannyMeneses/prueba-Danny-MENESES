function updateheader(){
    function mainFunction(printText) {
        document.getElementById("caja")
        caja="CABECERA ACTUALIZADA";
    }
    function printText() {
        document.getElementById("caja")
        alert = caja();
        if (!$('caja').val()) {
            alert('Ups! Nada que mostrar');
        }
    }
}
return caja,alert;

function mainFunction(printText) {
    document.getElementById("caja")
    caja="CABECERA ACTUALIZADA";
}
function printText() {
    document.getElementById("caja")
    alert = caja();
    if (!$('caja').val()) {
        alert('Ups! Nada que mostrar');
    }
}